name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: pharmatrack
          POSTGRES_PASSWORD: pharmatrack
          POSTGRES_DB: pharmatrack
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U pharmatrack -d pharmatrack"
          --health-interval 5s --health-timeout 5s --health-retries 10

    env:
      DATABASE_URL: postgresql://pharmatrack:pharmatrack@localhost:5432/pharmatrack
      AUTH_SECRET: ci-test-secret-not-used-anywhere-real

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9.6.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Format check
        run: pnpm format:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Apply DB migrations
        run: pnpm db:migrate

      - name: Verify schema matches migrations (no drift)
        run: |
          pnpm --filter @pharmatrack/db generate
          if ! git diff --quiet packages/db/drizzle; then
            echo "::error::Schema and migrations are out of sync. Run 'pnpm db:generate' locally and commit the result."
            git diff packages/db/drizzle
            exit 1
          fi

      - name: Build
        run: pnpm build
